services:
  app:
    image: resume-screening-agent
    build: .
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - .:/app:cached
    command: uvicorn main:app --host 0.0.0.0 --port 8000
